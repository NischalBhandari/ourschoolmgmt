{% extends 'base.html.twig' %} 
{% block body %}
<div class="container content">
  <div class="row">
    <div class="col-md-12">
      <div class="card bg-gradient-info bg-info text-white mb-3 mt-3">
        <div class="card-body ">
          <div class="card-header h3 ">{{staff.name}}</div>
          {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <ul class="profile left">
            <li> Address : {{ staff.Address }}</li>
            <li> Staff Id : {{ staff.staffId }}</li>
            <li class ="bold"> Email: {{ staff.email }}</li>
            <li> Phone: {{ staff.phone }}</li>
            </ul>
          {% else %}
        </div>

      </div>
    
      <a href="{{ path('app_login') }}" class="btn btn-info">
        You need to login to see the list 😜😜 >></a
      >
       {% endif %}
    </div>
  </div>
</div>
{% endblock %}